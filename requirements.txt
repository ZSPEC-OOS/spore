# SPORE NLWS – Python dependencies
#
# Core (required for server.py and the CLI)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Optional – enables live DuckDuckGo search (falls back to mock if absent)
duckduckgo-search>=6.0.0

# Optional – enables external AI model enrichment during web crawls
openai>=1.0.0

# ── Activation pipeline (spore/activation_pipeline/) ──────────────────────
# Core — mechanistic interpretability + activation hooks
transformer_lens>=2.0.0

# Core — tensor ops (already a transitive dep of transformer_lens)
torch>=2.1.0

# Progress bars
tqdm>=4.66.0

# Optional — stream openwebtext corpus
# datasets>=2.19.0

# Optional — nnsight alternative hook backend
# nnsight>=0.3.0

# ── Dimensionality reduction (spore/activation_pipeline/reduction.py) ──────
umap-learn>=0.5.6
scikit-learn>=1.4.0
pandas>=2.1.0
numpy>=1.26.0

# Optional — Parquet I/O for save_projection / load_projection
# pyarrow>=15.0.0
